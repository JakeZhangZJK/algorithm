import reverseByWorld from '../../code/string/reverse-word'

test('reverseByWorld:Let\'s take LeetCode contest', () => {
  expect(reverseByWorld("Let's take LeetCode contest")).toBe("s'teL ekat edoCteeL tsetnoc")
})
